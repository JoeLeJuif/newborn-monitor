# Copier vers .env.local (ignoré par Git) et remplir avec les valeurs du
# projet Supabase « newborn-monitor ». Uniquement la clé publiable (anon) —
# jamais la clé service_role ni le mot de passe de la base.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
